import React,{useState} from 'react';
import {Activity,BookOpen,CalendarCheck,ChevronRight,ClipboardCheck,GraduationCap,LayoutDashboard,Menu,Target,Bell,X} from 'lucide-react';
import {useLocation,useNavigate} from 'react-router-dom';
const items=[['/dashboard','الرئيسية',LayoutDashboard],['/dashboard/lessons','الشرح والمحتوى',BookOpen],['/dashboard/assignments','تصحيح الواجبات',ClipboardCheck],['/dashboard/exams','الامتحانات',GraduationCap],['/dashboard/grades','درجاتي وتحليلي',Target],['/dashboard/attendance','حضوري',CalendarCheck],['/dashboard/notifications','الإشعارات',Bell],['/dashboard/analytics','التحليلات',Activity]] as const;
export default function StudentPortalPage({children}:{children:React.ReactNode}){const nav=useNavigate();const loc=useLocation();const [open,setOpen]=useState(false);const go=(p:string)=>{setOpen(false);nav(p)};return <div dir="rtl" className="min-h-screen bg-[#07090f]"><aside className={`fixed right-0 top-0 z-[120] h-screen w-72 border-l border-white/[.07] bg-[#0b0f16] p-5 shadow-2xl transition-transform duration-300 lg:translate-x-0 ${open?'translate-x-0':'translate-x-full'}`}><div className="flex items-center justify-between"><button onClick={()=>go('/dashboard')} className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-gradient-to-br from-violet-400 to-indigo-500 font-black text-black">أ</span><span className="text-right"><b className="block text-sm text-white">منصة أ. أحمد</b><small className="text-[10px] text-slate-500">مساحة الطالب</small></span></button><button onClick={()=>setOpen(false)} className="lg:hidden text-slate-500"><X/></button></div><nav className="mt-8 space-y-1.5">{items.map(([path,label,Icon])=>{const active=loc.pathname===path;return <button key={path} onClick={()=>go(path)} className={`flex w-full items-center gap-3 rounded-2xl px-4 py-3.5 text-right text-sm font-black transition ${active?'bg-violet-400/10 text-violet-300 shadow-inner':'text-slate-500 hover:bg-white/[.04] hover:text-white'}`}><Icon size={19}/><span>{label}</span>{active&&<ChevronRight className="mr-auto" size={15}/>}</button>})}</nav><div className="absolute bottom-5 left-5 right-5 rounded-2xl border border-emerald-400/10 bg-emerald-400/[.04] p-4 text-xs leading-6 text-slate-500"><b className="text-emerald-300">تحليل مستواك</b><br/>كلما زادت بياناتك، أصبحت توصيات المنصة أدق.</div></aside><div className="lg:pr-72"><div className="sticky top-0 z-[90] flex items-center justify-between border-b border-white/[.07] bg-[#07090f]/90 px-4 py-3 backdrop-blur-xl lg:hidden"><button onClick={()=>setOpen(true)} className="rounded-xl bg-white/[.05] p-2 text-white"><Menu/></button><b className="text-sm text-white">مساحة الطالب</b><button onClick={()=>go('/dashboard')} className="text-xs font-black text-violet-300">الرئيسية</button></div>{children}</div></div>}
