import React,{useEffect,useState}from'react';
import{BookOpen,CheckCircle2,ClipboardCheck,GraduationCap,Target,Bell,PlayCircle,ExternalLink,FileText}from'lucide-react';
import{Link}from'react-router-dom';
import{supabase}from'../lib/supabase';
import{useAuthStore}from'../store/authStore';

type Row=Record<string,any>;type Section='lessons'|'assignments'|'exams'|'grades'|'notifications';
function youtubeId(value:string){const raw=value.trim();if(!raw)return'';if(/^[A-Za-z0-9_-]{11}$/.test(raw))return raw;try{const u=new URL(raw);if(!/(^|\.)youtube(?:-nocookie)?\.com$/.test(u.hostname)&&!u.hostname.endsWith('youtu.be'))return'';if(u.hostname.endsWith('youtu.be'))return u.pathname.split('/').filter(Boolean)[0]||'';const q=u.searchParams.get('v');if(q)return q;const parts=u.pathname.split('/').filter(Boolean);const marker=parts.findIndex(p=>p==='embed'||p==='shorts'||p==='live');return marker>=0?parts[marker+1]||'':parts.at(-1)||''}catch{return''}}
export default function StudentSectionPage({section}:{section:Section}){const{profile}=useAuthStore();const id=profile?.id||'';const[rows,setRows]=useState<Row[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{let live=true;(async()=>{setLoading(true);setError('');let result:any;if(section==='lessons')result=await supabase.from('lessons').select('id,title,description,content,video_provider,video_asset_id,pdf_storage_path,display_order').eq('status','published').order('display_order').limit(200);if(section==='assignments')result=await supabase.from('assignments').select('id,title,description,deadline,max_score,is_published,lesson_id').eq('is_published',true).order('deadline',{ascending:true}).limit(200);if(section==='exams')result=await supabase.from('exams').select('id,title,description,max_score,starts_at,ends_at,result_mode,pass_percentage').eq('is_published',true).order('starts_at',{ascending:false}).limit(100);if(section==='grades')result=await supabase.from('exam_attempts').select('id,exam_id,attempt_number,status,score,submitted_at,created_at').eq('student_id',id).order('created_at',{ascending:false}).limit(100);if(section==='notifications')result=await supabase.from('notifications').select('id,title,message,body,type,created_at,is_read,is_consumed').eq('recipient_id',id).order('created_at',{ascending:false}).limit(100);if(live){if(result?.error)setError('تعذر تحميل البيانات.');setRows(result?.data||[]);setLoading(false)}})();return()=>{live=false}},[id,section]);const titles={lessons:['الشرح والمحتوى','الدروس المنشورة والفيديوهات المتاحة لك'],assignments:['الواجبات','الواجبات المنشورة والمطلوب منك تسليمها'],exams:['الامتحانات','الاختبارات المتاحة لك ونتائج محاولاتك'],grades:['درجاتي','سجل نتائجك وتحسن مستواك'],notifications:['الإشعارات','كل جديد يخص حسابك']}as const;const[title,sub]=titles[section];return <main dir="rtl" className="min-h-screen bg-[#07090f] px-3 py-4 text-white sm:px-6 lg:px-8"><div className="mx-auto max-w-[1200px]"><header className="mb-5 rounded-[1.5rem] border border-white/[.07] bg-[#0d1118] p-4 sm:mb-7 sm:rounded-[2rem] sm:p-6"><div className="flex items-center gap-3"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-violet-400/10 text-violet-300"><Icon section={section}/></span><div className="min-w-0"><p className="text-[11px] font-black text-violet-300">مساحة الطالب</p><h1 className="mt-1 text-xl font-black sm:text-2xl">{title}</h1><p className="mt-1 text-xs leading-5 text-slate-500">{sub}</p></div></div></header>{loading?<div className="rounded-3xl border border-white/[.07] bg-[#0d1118] p-10 text-center text-slate-500">جاري تحميل البيانات...</div>:error?<div className="rounded-3xl border border-red-400/10 bg-[#0d1118] p-8 text-center text-red-300">{error}</div>:rows.length===0?<div className="rounded-3xl border border-dashed border-white/[.08] bg-[#0d1118] p-10 text-center text-slate-500">لا توجد بيانات حتى الآن.</div>:<div className="grid gap-3">{rows.map((r,i)=><RowView key={r.id||i} row={r} section={section}/>)}</div>}</div></main>}
function Icon({section}:{section:Section}){const C={lessons:BookOpen,assignments:ClipboardCheck,exams:GraduationCap,grades:Target,notifications:Bell}[section];return <C size={22}/>}
function RowView({row,section}:{row:Row;section:Section}){if(section==='lessons'){const id=youtubeId(String(row.video_asset_id||''));return <article className="overflow-hidden rounded-3xl border border-white/[.07] bg-[#0d1118] p-4 sm:p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0 flex-1"><h3 className="break-words font-black">{row.title}</h3><p className="mt-2 whitespace-pre-wrap break-words text-sm leading-7 text-slate-400">{row.description||row.content||'درس متاح للمذاكرة.'}</p></div><BookOpen className="shrink-0 text-violet-300" size={20}/></div>{id&&<div className="mt-4 overflow-hidden rounded-2xl border border-white/[.06] bg-black"><div className="aspect-video w-full"><iframe className="h-full w-full" src={`https://www.youtube-nocookie.com/embed/${encodeURIComponent(id)}?rel=0&modestbranding=1&playsinline=1`} title={row.title||'فيديو الشرح'} loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen referrerPolicy="strict-origin-when-cross-origin"/></div></div>}{row.pdf_storage_path&&<div className="mt-3 rounded-xl bg-white/[.03] p-3 text-xs font-bold text-slate-300"><FileText className="ml-2 inline" size={15}/>ملف PDF متاح مع الدرس</div>}{id&&<a href={`https://www.youtube.com/watch?v=${encodeURIComponent(id)}`} target="_blank" rel="noreferrer" className="mt-3 inline-flex min-h-10 items-center gap-2 text-xs font-black text-violet-300"><PlayCircle size={15}/>فتح الفيديو في YouTube<ExternalLink size={13}/></a>}</article>}
if(section==='assignments')return <article className="flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div className="min-w-0"><b className="break-words">{row.title||'واجب'}</b><p className="mt-2 text-xs text-slate-500">{row.deadline?`آخر موعد: ${new Date(row.deadline).toLocaleString('ar-EG')}`:'بدون موعد محدد'}</p></div><span className="rounded-full bg-sky-400/10 px-3 py-2 text-xs font-black text-sky-300">{row.max_score} درجة</span></article>;
if(section==='exams')return <article className="flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div className="min-w-0"><b className="break-words">{row.title}</b><p className="mt-2 text-xs text-slate-500">{row.starts_at?new Date(row.starts_at).toLocaleString('ar-EG'):'متاح حسب الجدول'}</p></div><Link to={`/dashboard/exams/${row.id}`} className="min-h-10 rounded-xl bg-amber-300 px-4 py-2 text-xs font-black text-black">فتح الامتحان</Link></article>;
if(section==='grades')return <article className="flex items-center justify-between rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div><b>محاولة امتحان</b><p className="mt-2 text-xs text-slate-500">{row.submitted_at?new Date(row.submitted_at).toLocaleDateString('ar-EG'):row.status}</p></div><strong className="text-2xl">{row.score??'—'}</strong></article>;
return <article className="rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div className="flex items-start justify-between gap-3"><b>{row.title}</b>{row.is_read&&<CheckCircle2 size={17} className="text-emerald-300"/>}</div><p className="mt-2 text-sm leading-7 text-slate-400">{row.message||row.body}</p><p className="mt-2 text-xs text-slate-600">{new Date(row.created_at).toLocaleString('ar-EG')}</p></article>}
