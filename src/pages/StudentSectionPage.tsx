import React,{useEffect,useState} from 'react';
import {BookOpen,CalendarCheck,CheckCircle2,ClipboardCheck,FileText,GraduationCap,Target,Bell} from 'lucide-react';
import {supabase} from '../lib/supabase';
import {useAuthStore} from '../store/authStore';

type Row=Record<string,any>; type Section='lessons'|'assignments'|'exams'|'grades'|'attendance'|'notifications';
export default function StudentSectionPage({section}:{section:Section}){const {profile}=useAuthStore();const id=profile?.id||'';const [rows,setRows]=useState<Row[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{let live=true;(async()=>{setLoading(true);setError('');let result:any;if(section==='lessons')result=await supabase.from('lessons').select('id,title,description,content,video_provider,video_asset_id,display_order').eq('status','published').order('display_order').limit(200);if(section==='assignments')result=await supabase.from('assignment_submissions').select('id,assignment_id,status,submitted_at,score,created_at').eq('student_id',id).order('created_at',{ascending:false});if(section==='exams')result=await supabase.from('exams').select('id,title,max_score,starts_at,ends_at,result_mode,pass_percentage').order('starts_at',{ascending:false}).limit(100);if(section==='grades')result=await supabase.from('exam_attempts').select('id,exam_id,attempt_number,status,score,submitted_at,created_at').eq('student_id',id).order('created_at',{ascending:false}).limit(100);if(section==='attendance')result=await supabase.from('attendance').select('id,session_id,status,marked_at').eq('student_id',id).order('marked_at',{ascending:false}).limit(100);if(section==='notifications')result=await supabase.from('notifications').select('id,title,message,type,created_at,is_read,is_consumed').eq('recipient_id',id).order('created_at',{ascending:false}).limit(100);if(live){if(result?.error)setError(result.error.message);setRows(result?.data||[]);setLoading(false)}})();return()=>{live=false}},[id,section]);const titles={lessons:['الشرح والمحتوى','كل الدروس المنشورة المتاحة لك'],assignments:['تصحيح الواجبات','متابعة ما تم تسليمه والدرجات'],exams:['الامتحانات','الاختبارات المتاحة ونتائج المحاولات'],grades:['درجاتي','سجل نتائجك وتحسن مستواك'],attendance:['حضوري','سجل الحضور والتأخر'],notifications:['الإشعارات','كل جديد يخص حسابك']} as const;const [title,sub]=titles[section];return <main dir="rtl" className="min-h-screen bg-[#07090f] px-4 py-6 text-white sm:px-6 lg:px-8"><div className="mx-auto max-w-[1200px]"><header className="mb-7 rounded-[2rem] border border-white/[.07] bg-[#0d1118] p-6"><div className="flex items-center gap-3"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-violet-400/10 text-violet-300"><Icon section={section}/></span><div><p className="text-xs font-black text-violet-300">مساحة الطالب</p><h1 className="mt-1 text-2xl font-black">{title}</h1><p className="mt-1 text-xs text-slate-500">{sub}</p></div></div></header>{loading?<div className="rounded-3xl border border-white/[.07] bg-[#0d1118] p-12 text-center text-slate-500">جاري تحميل البيانات...</div>:error?<div className="rounded-3xl border border-red-400/10 bg-[#0d1118] p-8 text-center text-red-300">تعذر تحميل الصفحة.</div>:rows.length===0?<div className="rounded-3xl border border-dashed border-white/[.08] bg-[#0d1118] p-12 text-center text-slate-500">لا توجد بيانات حتى الآن.</div>:<div className="grid gap-3">{rows.map((r,i)=><RowView key={r.id||i} row={r} section={section}/>)}</div>}</div></main>}
function Icon({section}:{section:Section}){const C={lessons:BookOpen,assignments:ClipboardCheck,exams:GraduationCap,grades:Target,attendance:CalendarCheck,notifications:Bell}[section];return <C size={23}/>}
function RowView({row,section}:{row:Row;section:Section}){if(section==='lessons')return <article className="rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div className="flex items-center justify-between gap-3"><div><h3 className="font-black">{row.title}</h3><p className="mt-2 text-sm leading-7 text-slate-500">{row.description||row.content||'درس متاح للمذاكرة.'}</p></div><BookOpen className="shrink-0 text-violet-300" size={20}/></div>{row.video_asset_id&&<div className="mt-3 rounded-xl bg-violet-400/5 p-3 text-xs font-bold text-violet-300">فيديو شرح متاح</div>}</article>;if(section==='assignments')return <article className="flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div><b>واجب #{String(row.assignment_id).slice(0,8)}</b><p className="mt-2 text-xs text-slate-500">{row.submitted_at?new Date(row.submitted_at).toLocaleDateString('ar-EG'):'لم يُسلّم بعد'}</p></div><span className="rounded-full bg-emerald-400/10 px-3 py-2 text-xs font-black text-emerald-300">{row.score!=null?`الدرجة ${row.score}`:row.status}</span></article>;if(section==='exams')return <article className="flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div><b>{row.title}</b><p className="mt-2 text-xs text-slate-500">{row.starts_at?new Date(row.starts_at).toLocaleString('ar-EG'):''}</p></div><span className="rounded-full bg-sky-400/10 px-3 py-2 text-xs font-black text-sky-300">من {row.max_score}</span></article>;if(section==='grades')return <article className="flex items-center justify-between rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div><b>محاولة امتحان</b><p className="mt-2 text-xs text-slate-500">{row.submitted_at?new Date(row.submitted_at).toLocaleDateString('ar-EG'):row.status}</p></div><strong className="text-2xl">{row.score??'—'}</strong></article>;if(section==='attendance')return <article className="flex items-center justify-between rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><div><b>جلسة حضور</b><p className="mt-2 text-xs text-slate-500">{row.marked_at?new Date(row.marked_at).toLocaleDateString('ar-EG'):''}</p></div><span className="font-black text-emerald-300">{row.status}</span></article>;return <article className="rounded-3xl border border-white/[.07] bg-[#0d1118] p-5"><b>{row.title}</b><p className="mt-2 text-sm leading-7 text-slate-400">{row.message}</p><p className="mt-2 text-xs text-slate-600">{new Date(row.created_at).toLocaleString('ar-EG')}</p></article>}
